import React from 'react';
import PropTypes from 'prop-types';

const Confirm = props => {
    return (
        <div>
            confirm
        </div>
    );
};

Confirm.propTypes = {
    
};

export default Confirm;